# When we import module than the nn function is working in that module will excute in our file
# To avoid this we use  if__name__ == "__main__"  funtion in that module

import jaimin
jaimin.welcome()